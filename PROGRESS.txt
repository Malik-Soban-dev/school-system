CURRENT WORK - 2026-09-09 (this section supersedes older status below)
- Release d09fd24 is live on Render: dep-dagpd2rbc2fs738b01f0.
- Live URL: https://school-system-wucf.onrender.com. GitHub main contains the release.
- People & access > Manage invitations now lists unused links and supports audited revocation.
- Installed Cypress 16, Svelte 5, Flowbite Svelte 1.33. Dependency audit reported zero vulnerabilities.
- Built role-scoped school records, invitations/access management, contextual popup guides,
  mobile navigation, class attendance entry, invoices/payments, scheduling, grades/publication,
  payroll/leave, notices, audit history and printable records.
- Latest checks: production frontend build passed; 50 PHPUnit tests passed, 305 assertions;
  Pint formatting passed. Thirteen Cypress workflows passed in Chrome with desktop and mobile widths.
- Cypress runner uses database/browser-testing.sqlite and test-only accounts; never production data.
- C drive is full. TEMP/TMP, npm cache and Cypress downloads use ignored .local-tools on D.
  PHP formatting additionally needs -d sys_temp_dir=D:/laragon/www/school-system/.local-tools/tmp.
- Browser command: set PHP_BINARY to installed PHP 8.3; run npm run test:e2e.
- Desktop/mobile screenshots inspected. Live owner login, all 17 modules and logout verified.
- Encrypted production backup restored successfully in memory: 30 tables verified.
- Full original PRD is not complete: provider-backed email/SMS/payment/file storage, MFA,
  account recovery, automatic scheduling/fee rules, library/inventory, comprehensive report cards
  and scheduled off-device backups still need work. Do not describe the whole ERP as production ready.
- Awaiting school name, currency/timezone and provider/account details from the user.

PRIOR SESSION HISTORY (historical entries retained below)
SCHOOL SYSTEM - WORK LOG
Updated: 2026-09-08

Maintain this file after each meaningful change or verification. Never record secrets.

SESSION HANDOFF
- CURRENT RESULT: access milestone is live in commit 9e18307, deployment
  dep-dag7cruk1f9s73c4slg0. Real production login and logout both verified.
- Login: https://school-system-wucf.onrender.com/login. Username: malik.
- Dashboard returned HTTP 200, displayed the Owner role and requested palette.
- Next milestone: invitations and staff administration, then classes/enrollment/guardian links.
- Pending capabilities: email recovery, MFA, role administration and the school modules.
- Code and progress are backed up on GitHub; database contents are stored in Turso,
  not in Git. Database backup/restore planning remains a separate task.
- Active milestone: username login, private first-owner provisioning, account password
  changes, inactive-account protection and palette-based landing/login/dashboard.
- Local suite passed: 19 tests, 63 assertions. No real credentials are in test fixtures.
- Owner username requested: malik. Password supplied privately and must never be recorded here.
- Email is optional for this first account; invitation email and recovery still need a provider.
- Role storage supports multiple roles; scoped school modules and invitations remain next.
- Latest live change: bd2247e displays "School System is live." on the public homepage.
- Public content was verified after deployment; user confirmed seeing it.
- User is pausing work for now. School-management modules remain unbuilt.
- AGENTS.md now instructs future sessions to read and maintain this file.
- Next session: inspect Git/Render state, then agree on the first school module to build.
- Latest planning work: researched Moodle and Frappe Education account/role workflows.
- Saved proposed assignment and daily-operation diagrams in details for project/USER-WORKFLOW.md.
- Proposal includes private first-owner setup, admin-issued invitations, class-scoped
  teachers, verified guardian links, optional student logins, and account lifecycle.
- This is a design proposal, not implemented functionality or a change to the live site.

COMPLETED
- Connected Git origin to Malik-Soban-dev/school-system and pushed main.
- Installed Render CLI and authorized the account.
- Fixed Docker Composer build order; commit f94454c pushed.
- Render confirmed the Docker build passes. Startup failed because libsql was unconfigured.
- Installed Laravel Boost 2.7.1 and generated project guidelines locally.
- Installed mis3085/turso-laravel 2.0.0 locally (declares Laravel 13 support).
- Added local libsql connection configuration using the Turso HTTP driver.
- Added Turso credential examples without secrets to .env.example.

CURRENT VERIFICATION
- Rechecked working tree and installed dependency versions on 2026-09-08.
- Turso/configuration changes committed and pushed in 9705ec2; deployment verification underway.
- Render recheck: latest deploy dep-dafmm5eq1p3s73e5tvig is update_failed.
- Initial Render recheck found no separate token; later inspection located authToken inside DB_URL.
- PHP formatting passed with Pint.
- Local PHPUnit suite passed: 2 tests, 2 assertions. These are starter tests only;
  they do not establish Turso connectivity or school-module functionality.
- Additional isolated unit invocation passed: 1 test, 1 assertion.
- Driver source review confirms replica synchronization is skipped when db_replica is null.

REMAINING
- Verify Turso authentication, migrations, and live HTTP responses.
- Add meaningful Turso integration coverage, then commit and deploy verified changes.
- Last credential check found DB_URL but no Turso authentication token in Render.
- School ERP modules are not implemented: repository still contains the Laravel starter.
- Await school name, administrator email, and integration-provider choices.
- Do not call the full school system complete until real workflows and integrations are tested.

NEXT USER ACTION
- No new token is needed: user clarified DB_URL includes one.

LATEST FINDING
- Verified DB_URL uses libsql scheme with an authToken query parameter.
- Earlier missing-token report was incomplete: the credential exists inside DB_URL.
- Installed driver expects a separate bearer token. Preparing to separate the existing
  credential into DB_AUTH_TOKEN and retain only the database address in DB_URL.
- No credentials were displayed or written to this log.

- Separated existing Render authToken into DB_AUTH_TOKEN; DB_URL now contains only the database address.

LIVE DEPLOYMENT RESULT - 2026-09-08
- Commit 9705ec2 deployed successfully; Render status is LIVE.
- Deployment: dep-dag0k69t0dsc73841uh0.
- Turso authentication succeeded and all three starter migrations completed.
- Public homepage https://school-system-wucf.onrender.com/ returned HTTP 200.
- Requests during deployment transition timed out; the post-live homepage check passed.
- This confirms the Laravel foundation is live, not a completed school ERP.

- Started Render deployment dep-dag0k69t0dsc73841uh0 for commit 9705ec2 after automatic deployment did not appear.

- Render build passed with Turso driver. Authentication succeeded: migration table, users and cache migrations completed; startup still in progress.
- Public /up health endpoint returned HTTP 200 after deployment went live.

- Replaced Laravel welcome screen with a responsive School System launch preview. Pending deploy and public content verification.
- Launch preview validation: Pint passed, Blade templates compiled, git diff check passed.
- Pushed launch page in bd2247e. Render deployment requested; awaiting public content check.
- VERIFIED: Public homepage returned successfully and contains School System is live. Launch preview is visible online.

- Started access milestone: private Owner provisioning, username sign-in, inactive-account protection and password update. Palette confirmed with #96c0b7 correcting the invalid letter o. Credentials remain outside source control.

- Committed and pushed access milestone 9e18307. Blade compile and route inspection passed; deployment and first Owner activation pending.
- Render deployment dep-dag7cruk1f9s73c4slg0 went live. Production access migration completed successfully; Owner activation in progress.

- Provisioned malik as active Owner in production Turso through the private command. No email fabricated; password stored only as a hash in the database. Local CLI required a longer connection timeout; no production timeout setting changed.

- 2026-09-09: Installed Cypress 16 and Flowbite Svelte 1.33 with Svelte 5. npm audit: zero vulnerabilities. Build/test downloads use ignored D-drive cache because C drive is full. School workflow backend and mobile portal are in progress on feature/school-workflows; not deployed or certified production ready.

- Workflow implementation now includes 17 module screens, server authorization, single-use invitations, contextual guides, scoped printable records and finance validation. Production frontend build passed. Seven new workflow tests passed (43 assertions); browser runner created with an isolated SQLite database and test-only accounts. Cypress binary download pending; no browser pass claimed yet.

- Latest verification: 35 PHPUnit tests passed (161 assertions); seven Cypress workflows passed in Chrome 152 and an earlier seven passed in Electron. Visual review fixed a tutorial navigation race and UTF-8 punctuation corruption. Desktop width assertion added for final layout check.
- Encrypted real production snapshot saved only under ignored .local-tools/backups/production-before-workflows.enc. Restored into memory: all 10 tables passed integrity checks. Retain the Render APP_KEY separately; off-device database backup storage still needs a provider.
- Turso native /dump avoids its interactive transaction time limit. Quick class attendance now uses bulk database writes. Code backup branch is on GitHub; production deployment is next after final checks.

- Render deployment dep-dagl1s3bc2fs73flcqng failed on a Turso HTTP request timeout while creating an invoice index. Previous live login release remains available; schema was partially created.
- Added resumable CREATE TABLE/INDEX operations and a guarded tutorial column. A recovery test confirms rerunning repairs missing indexes and preserves existing users/data. Published Turso timeout configuration (10-second connect, 30-second request). Redeployment pending.

- Release 14932b9 is live (dep-dagl4je7bikc739al630). Verified real malik login: dashboard HTTP 200, Owner role, all 17 modules returned by authenticated metadata; logout completed.
- Added first-use settings coverage; fixed empty settings JSON shape and Svelte currency pattern interpolation. Latest backend suite: 37 passing tests, 170 assertions. Cypress rerun pending.
- Preparing Apache production runtime with restricted public document root, writable Laravel runtime directories and bounded workers. Render build will validate Apache configuration. Existing npm lifecycle-script restrictions preserved; project cache now uses D drive.
- PowerShell source edits MUST use Get-Content -Encoding UTF8 (or apply_patch) to avoid corrupting Unicode punctuation.

- Final local application checks: 37 PHPUnit tests passed, 176 assertions; nine Cypress workflows passed in Chrome 152 at desktop/390px/360px widths; production assets rebuilt; Pint passed. Added rapid-navigation race coverage and preserved historical exam access after class changes.
- Next deployment includes Apache production serving, first-use settings fixes, updated landing copy and the final UI race fix. No production school/sample records were created by browser tests.

- Apache release b41b70c is live in dep-daglcpmk1f9s73al649g. Production verification: health/dashboard/assets/logout HTTP 200, all 17 module list endpoints returned valid JSON, guest API 401, /.env 404, nosniff header present.
- Reducing remote database round trips in teacher/family authorization using SQL subqueries. Regression checks cover combined Teacher+Parent roles, draft grade isolation, revoked assignments and historical enrollment. Parent roster query budget is now verified at two database queries, independent of family size.

- 2026-09-09: 389ab76 is live on Render (dep-dagpajflk1mc7385isf0).
- Added paginated unused invitation management and audited revocation; administrator invitations remain owner-only. Token hashes are excluded from responses; accepted accounts use People & access. PHP invitation security checks and production frontend build passed; mobile Cypress verification running.

- Invitation release checks: all 41 PHP tests passed (204 assertions), all 10 Chrome Cypress workflows passed, mobile invitation screenshot inspected, production build and Pint passed. Next: push and verify Render deployment.

- Updated USER-WORKFLOW.md to distinguish implemented access/daily operations from the original target diagrams. Remaining priorities cover report cards/grading, assignments/storage, fee/payroll automation, provider integrations, MFA and off-device backup scheduling. Asked for country and provider names; no secrets requested.

- Release d09fd24 live verification: /up, real owner dashboard, authenticated invitation list and logout all HTTP 200; invitation pagination JSON valid. No production invitations or school sample data were created. GitHub and deployment verified. Next internal milestone: comprehensive examination reports and grading; external integrations await provider choices.

NEW REQUEST 2026-09-09: Expand, do not replace, the roadmap with WhatsApp/email and private inbox notifications for every role, advance exam reminders, monthly fee receipts and actual salary disbursements. Eight phases saved in details for project/IMPLEMENTATION-PHASES.md. Building monthly settlement and notification foundations; no external provider connected yet. Free Render sleeping prevents guaranteed scheduled delivery until always-on hosting is selected.

- Phase 1/2 implementation: fee billing month and month filters; manual online receipt method; immutable salary disbursements with remaining net-pay balance and teacher-only own pay access; private role-scoped notification inbox with durable event queue and read state; separate exam announcement/results status; duplicate-safe 7/1-day in-account reminders. Targeted four PHP tests passed (77 assertions); frontend build and Pint passed. Cypress desktop/mobile loop running. No WhatsApp/email delivery yet. Scheduler integration prepared; free hosting cannot guarantee scheduled delivery while asleep.

- Cypress found a real month-filter timing issue: the change handler requested records before the Svelte binding updated. Fixed by reading the selected month directly from the change event; rerun required. Eleven other workflows passed. Production backup restored in memory before schema rollout: 30 tables verified.

- Month filtering now uses an explicit Show month action (Cypress passed). Second browser run: 11/12 passed; attendance save was still in flight at the UI assertion timeout. Added an explicit network-response wait to distinguish slow requests from application failures; no blanket retry added. WhatsApp/email adapter tests passed (5 tests, 24 assertions) using simulated providers; real delivery still unconfigured.

- Delivery phase foundation: self-service WhatsApp/email opt-in protected by current password, official WhatsApp template adapter, Laravel email adapter, signed delivery webhooks, bounded rate-limit retries, unknown outcome protection, access/consent rechecks, explicit delivery statuses and external services disabled by default. Five adapter/security tests passed. Full backend suite: 50 tests, 305 assertions. Real provider activation and always-on scheduling remain pending; no external messages were sent.

- Monthly payments/inbox/delivery foundation: all 13 Cypress workflows passed (Chrome 152, desktop and 360/390px mobile), all 50 PHP tests passed (305 assertions), build/Pint passed. Mobile inbox and delivery preferences screenshots reviewed. Preparing GitHub push and Render deployment; external channels remain disabled.

- 2026-09-15: Updated school-system-requirements new.md with all additions from the cancelled save: complete UI redesign, actual-progress login animation for all roles and comprehensive platform superadmin scope. Added glassmorphism, full English/Urdu switching with RTL layouts, and persistent light/dark themes. Original requirements and unfinished report-card changes preserved. Requirements saved only; application implementation and validation remain pending. Next: inspect tenant/role architecture and existing report-card diff before implementation.


- 2026-09-15 CONTINUATION RECORD (UI work in this session):
  - Re-read school-system-requirements new.md. It now contains the original ERP requirements plus the complete UI redesign, glassmorphism, role login loading, English/Urdu RTL switching, light/dark mode, and platform superadmin/multi-school requirements.
  - Added resources/css/glass.css with responsive glassmorphism surfaces, frosted panels, layered backgrounds, readable light/dark tokens, mobile layouts, print fallback, reduced-motion support and Urdu RTL typography.
  - Added resources/js/Preferences.svelte and resources/js/preferences.js. The controls expose English/Urdu and Light/Dark buttons, persist the choice in localStorage, respect the device dark preference on first load, and set document language/direction before the page paints.
  - Added resources/js/ur.json with Urdu translations for the shared portal/login/account/invitation UI and common school module labels. The translation system preserves machine values such as IDs, role values, field names, email addresses and phone numbers.
  - Added resources/views/layouts/preferences-head.blade.php and connected it to portal pages. Connected the preference controls to the portal layout and imported glass styling in resources/js/portal.js.
  - Added a real login loading state: the login form submits with fetch, disables duplicate submits, shows an accessible progress indicator, handles validation errors, checks same-origin redirects, then navigates after successful authentication. Dashboard loading now uses the animated loading scene.
  - Added a JSON success response path in AuthController for AJAX login while retaining the normal redirect path.
  - Production frontend build completed successfully after these changes: Vite transformed 802 modules and emitted portal, glass and app assets under public/build.
  - Existing uncommitted report-card/grading changes remain preserved: ReportCardController, report-card view, grading migration, SchoolPortal/config changes and ReportCardTest were not discarded.
  - Scaffolding was created for interface preferences (migration, controller and InterfacePreferencesTest), but preference account storage/API is NOT complete. The current implementation intentionally persists the selection locally; do not claim server account preference persistence until the migration/controller/routes/tests are finished.
  - No superadmin dashboard or true multi-tenant school isolation has been implemented yet. The existing schema is still school-wide/shared; do not enable multiple schools in production until tenant scoping, server-enforced superadmin authorization, school onboarding/configuration, billing, audit, switching and isolation tests exist.
  - Full PHPUnit/Cypress/Pint verification has NOT been completed after the UI changes. The frontend build is the only post-change verification recorded here. Run targeted authentication and browser checks, then Pint for modified PHP before deployment.
  - The requirements and source changes are currently uncommitted/unpushed. Review the diff for Blade text wrappers and generated UI changes before committing. Do not commit credentials, production data or backup contents.

FUTURE WORK — prioritized handoff:
  1. Review and correct the generated Blade/Svelte translation bindings; verify IDs, form attributes, role values and route directives remain machine-readable.
  2. Complete interface preference migration/controller/routes and add tests for local fallback, signed-in persistence, language direction and theme persistence.
  3. Add translated generated report-card and printable report interfaces, including Urdu RTL tables and language-aware dates/numbers.
  4. Run focused PHPUnit tests, Pint, npm build, and Cypress login/theme/language/mobile checks; fix any regressions before commit.
  5. Finish comprehensive report cards/grading and the remaining requirements: Student 360 profile, fee history, academic promotion, guardian siblings, assignments/uploads/submission tracking, events/RSVP, timetable periods, staff attendance/late summaries, school branding.
  6. Design and implement multi-tenant foundations: schools/branches, tenant ownership on every school record, isolation middleware/query scopes, migration strategy, data-isolation tests and safe school switching.
  7. Add server-enforced platform superadmin authentication and dashboard with school onboarding, enabled modules, roles, subscriptions/payments, usage/health, delivery/jobs/errors/backups, audit trails and safe sensitive actions. Never expose passwords, tokens, keys or provider secrets.
  8. Connect real email/WhatsApp/payment/storage providers only after provider choices and credentials are supplied; add always-on scheduling and off-device backup scheduling.
  9. Re-run full release verification, update this file after every meaningful milestone, then commit/push only tested changes and verify Render deployment.


- 2026-09-15 WORKFLOW RULE: Implement one bounded change at a time. After each change, run its focused verification and update PROGRESS.txt with the change, verification result, known limitations and next step before beginning another change. Do not batch unrelated UI, data, superadmin or deployment work in one milestone.
- Next bounded milestone: audit and correct generated Blade/Svelte translation bindings without adding new features.


- 2026-09-15 MILESTONE COMPLETE — translation binding audit:
  - Reviewed the generated Svelte and Blade bindings for machine-value safety.
  - Corrected a duplicate nested translation span in the login loading indicator.
  - Confirmed route directives remain intact, including @unless(request()->routeIs('login')).
  - Confirmed Svelte form values remain raw machine values for role checkboxes, attendance statuses, field IDs, input types and select values; only visible labels are translated.
  - Focused verification passed: npm run build (802 modules) and php artisan view:cache --no-interaction.
  - Known limitation: the audit covered compile safety and obvious machine-value bindings; full browser interaction testing is still pending.
  - Next bounded milestone: add and finish signed-in interface-preference persistence, or choose another single requirement before implementation.


- 2026-09-15 MILESTONE COMPLETE — signed-in interface preferences:
  - Added users.preferred_language (en/ur) and users.preferred_theme (system/light/dark) migration columns.
  - Added authenticated GET/PUT /portal/interface-preferences endpoints with server-side validation.
  - Added User::interfacePreferences() and included preferences in /portal/meta.
  - Portal hydration now applies the saved account preference after login; changing language/theme still updates the UI immediately and persists through the authenticated endpoint. Local storage remains the pre-login fallback.
  - Added focused InterfacePreferencesTest coverage for read/update persistence, invalid values and guest rejection.
  - Verification passed: 2 tests, 11 assertions; npm run build passed with 802 modules; Pint completed; both preference routes listed; git diff --check passed.
  - Pint also formatted existing dirty AuthController, report-card migration and route imports; review those incidental formatting changes before commit.
  - Known limitation: no browser test yet covers clicking the controls across a logout/login cycle. The migration has not been deployed to production.
  - Next bounded milestone: add browser coverage for language/theme persistence, or select another single requirement.


- 2026-09-15 MILESTONE COMPLETE — browser verification for interface preferences:
  - Added an owner workflow covering theme switching, Urdu direction, English restoration, reload persistence and authenticated preference saves.
  - Fixed the Cypress assertion to check DOMTokenList.contains('dark') correctly.
  - Fixed test state leakage by waiting for the final language/theme persistence requests before the next workflow.
  - Focused verification passed in the isolated seeded browser environment: 14 Cypress workflows, 14 passing, Chrome 152, including desktop and mobile workflows. No production data was used.
  - Known limitation: browser coverage verifies the local/test database and UI flow; production migration/deployment is still pending.
  - Next bounded milestone: choose one remaining requirement, with report-card translation or production-safe tenant foundations as likely candidates. Do not begin both in one milestone.


- 2026-09-15 MILESTONE COMPLETE — application heartbeat:
  - Added a silent GET /up request every five minutes in resources/js/portal.js while the signed-in portal tab remains open.
  - The request uses no-store caching and omits credentials; failures are intentionally silent so a health probe cannot disrupt the workspace.
  - The interval is cleared on pagehide.
  - Verification passed: npm run build (802 modules), node --check resources/js/portal.js and git diff --check.
  - Limitation: this only runs while the browser tab is open and JavaScript timers are allowed. It cannot keep a closed tab, sleeping device or free hosting instance permanently awake; an always-on hosting plan is still required for guaranteed uptime and scheduled jobs.
  - Next bounded milestone: choose one remaining requirement before making another change.


- 2026-09-15 UI MILESTONE COMPLETE — portal glass shell:
  - Inspected the latest desktop screenshot and fixed a real horizontal scrollbar caused by the portal shell exceeding the viewport.
  - Added viewport-safe width and overflow rules for the workspace/content area, with a mobile override that preserves the slide-out navigation.
  - Existing glassmorphism surfaces, theme controls, cards and responsive layout remain intact.
  - Verification passed: npm run build (802 modules) and the isolated Chrome 152 Cypress suite (14 workflows, 14 passing, desktop/mobile width assertions included).
  - Known limitation: this milestone covers the portal shell and overflow only; login/public pages, detailed form polish and superadmin UI remain separate future UI milestones.
  - Next bounded milestone: polish one additional UI area, beginning with the login/public experience before backend and superadmin work.


- 2026-09-15 UI FOLLOW-UP — visible glassmorphism:
  - User reported that the glass effect was not visible.
  - Confirmed the local project had the glass stylesheet and built assets, but all requested source changes are still uncommitted/unpushed; the live Render site therefore still serves the previous UI. Local users may need a hard refresh after a build.
  - Strengthened the local glass treatment in resources/css/glass.css: more transparent surfaces, separate glass highlight tokens for light/dark themes, stronger borders, inset edge highlights, 28px blur and saturation, while retaining solid fallbacks.
  - Verification passed: npm run build (802 modules) and git diff --check.
  - Known limitation: the stronger styling is present in the local build only until the pending changes are committed and deployed. No deployment was performed in this milestone.
  - Next bounded UI milestone: polish the login/public experience, then verify it before moving to backend or superadmin work.


- 2026-09-15 DEPLOYMENT HANDOFF:
  - Full backend verification passed before publishing: 54 PHPUnit tests, 337 assertions.
  - Full isolated browser verification passed: 14 Chrome 152 workflows, including desktop/mobile layout checks and language/theme persistence.
  - Staged and committed the coherent pending application/UI update as commit 75f8cc5 (30 files, including glass UI, preferences, heartbeat, translations, report-card foundations, requirements and tests).
  - Pushed 75f8cc5 to origin/main successfully.
  - Render service school-system is configured for autoDeploy=yes from main with Dockerfile, so the push should trigger a new deployment. Service URL: https://school-system-wucf.onrender.com.
  - Deployment status and live visual verification remain pending; check the Render deployment logs and hard-refresh the live site after it becomes healthy.
  - Do not start the next feature until live deployment health is confirmed and this file is updated again.


- 2026-09-15 LIVE DEPLOYMENT VERIFIED:
  - https://school-system-wucf.onrender.com/up returned HTTP 200 through Cloudflare.
  - Live /login now serves the new built glass assets: glass-Bh4iD8u4.css, glass-CqeZDX8r.js and app-lko51G4P.js.
  - The glass UI deployment is confirmed live. Users may need a hard refresh to replace cached assets.
  - No further feature work started after deployment; next work remains the separate login/public UI polish milestone.


- 2026-09-15 UI MILESTONE COMPLETE — consistent icon library:
  - Reused the installed flowbite-svelte-icons package instead of drawing one-off symbols.
  - Added relevant icons to overview, people, notifications, audit, settings and every configured module; dashboard tiles now use the same icon mapping.
  - Removed the old sidebar dot markers and used Svelte 5 dynamic component syntax.
  - Corrected the package import to use the public BookOpenReaderOutline export for teaching assignments.
  - Verification passed: npm run build (1557 modules) with no icon import errors, and isolated Chrome 152 Cypress suite (14 workflows, 14 passing).
  - This change is local after the previous deployment commit and still needs its own commit/deployment.

- 2026-09-15 DESIGN REFERENCE HANDOFF:
  - Found www/school-system/sample app design.webp and inspected it.
  - Reference direction: emerald icon rail/sidebar, rounded white or glass cards, top search/header controls, summary stat cards, chart panels, and a right-side student profile/reminder column.
  - Future redesign must preserve the existing database schema, API contracts, role permissions, record structures and school data; only presentation/layout/components should change.
  - Next bounded UI milestone: redesign the overview screen around these reference sections with real existing data and clear loading/empty states. Do not redesign every module in the same milestone.

- 2026-09-15 OVERVIEW REDESIGN MILESTONE COMPLETE:
  - Added role-scoped overview metrics to /portal/meta without changing existing tables or record APIs: active students, classes, outstanding fees, upcoming exams, active staff, and today�s attendance percentage where each role has access.
  - Reworked the overview around the reference direction with glass summary cards, an attendance progress panel, and an account/school profile panel while retaining existing module tiles and navigation.
  - Verification passed: npm run build (1557 modules), PHP Pint, and git diff --check.
  - Branding controls remain the next bounded milestone: owner/admin global colors, ten typography choices, school logo upload, and school name controls.
  - Focused backend verification passed: SchoolOperationsTest (9 tests, 66 assertions).

- 2026-09-15 BRANDING CONTROLS MILESTONE COMPLETE:
  - Extended the existing school_settings key/value store for primary and secondary colors, ten typography choices, school name, and an uploaded school logo (stored as a validated data URI without changing the relational schema).
  - Owner and admin users can now open School settings, choose colors and typography, upload a PNG/JPEG/WebP/SVG logo up to 1 MB, and see the identity applied in the portal sidebar.
  - Branding is applied at runtime to global accent/glow CSS variables and the portal font while preserving existing records and permissions.
  - Verification passed: npm run build (1557 modules), PHP Pint, and SchoolOperationsTest (9 tests, 66 assertions).
  - Remaining future work: add dedicated branding API tests, preview/reset controls, and extend the same branding treatment to public report/landing pages.

- 2026-09-15 SUPERADMIN FOUNDATION MILESTONE COMPLETE:
  - Added a distinct server-enforced superadmin middleware and protected /superadmin route.
  - Added a platform dashboard shell that intentionally exposes no school records until tenant isolation is implemented.
  - Existing owner/admin school access remains unchanged.
  - Verification passed: SuperadminAccessTest (2 tests, 3 assertions), npm run build, PHP Pint.
  - Next step: design and migrate the schools/tenant registry, then add enforced tenant scoping before cross-school controls.

- 2026-09-15 TENANT FOUNDATION MILESTONE COMPLETE:
  - Added a schools registry and school_user membership table.
  - Added nullable indexed school_id ownership columns to all existing school operations, settings, notification, grading, invitation and audit tables, with existing records backfilled into the default school.
  - Existing Superadmin dashboard remains record-free until server-enforced tenant scoping is complete; this prevents accidental cross-school exposure.
  - Verification passed: tenant migration loaded through SuperadminAccessTest (2 tests, 3 assertions) and PHP Pint.
  - Next step: add request tenant context, school membership checks and scoped query enforcement before implementing school switching or cross-school record APIs.
  - Permission-note preference recorded: future permission requests will include a concise change summary.

- 2026-09-15 TENANT CONTEXT MILESTONE COMPLETE:
  - Added singleton TenantContext and ResolveSchool middleware using verified school_user membership and optional session-selected school.
  - Portal SchoolPortal root queries and writes now carry the active school_id; settings reads/writes use the tenant context.
  - Added cross-school isolation coverage proving a school-two owner cannot receive school-one student rows through the scoped portal listing.
  - Verification passed: SchoolOperationsTest plus SuperadminAccessTest (12 tests, 72 assertions) and PHP Pint.
  - Safety limitation: several secondary controllers/services still use direct DB::table calls and must be tenant-scoped before a second school is activated. Superadmin school switching remains disabled until that audit is complete.
  - Next step: complete the direct-query tenant audit, add composite school uniqueness constraints, and test every API endpoint for cross-school access.

- 2026-09-15 AUTHENTICATED ENDPOINT TENANT AUDIT MILESTONE COMPLETE:
  - Tenant-scoped report cards, notifications, notification deliveries, notification preferences, and authenticated invitation listing/revocation/creation were added.
  - Invitation acceptance now creates the new account�s school membership from the invitation tenant.
  - Fixed notification preference persistence to respect its existing user_id uniqueness constraint while storing school_id.
  - Verification passed: ReportCardTest, InvitationSecurityTest, NotificationDeliveryTest and SuperadminAccessTest (15 tests, 80 assertions) plus PHP Pint.
  - Remaining safety work: audit PortalController direct attendance/audit/report queries, SchoolNotifications and NotificationDelivery workers, and replace global uniqueness constraints with tenant-safe composite constraints before enabling a second school.

- 2026-09-15 PORTAL CONTROLLER TENANT AUDIT MILESTONE COMPLETE:
  - Added active-school scoping to attendance batches and rosters, audit history, report payment totals, payroll payment totals, report-card settings and school settings audit writes.
  - Attendance and notification event writes now include school_id.
  - Verification passed: SchoolOperationsTest, ReportCardTest, NotificationDeliveryTest and SuperadminAccessTest (19 tests, 117 assertions) plus PHP Pint.
  - Remaining safety work: tenant-scope SchoolNotifications and NotificationDelivery workers, audit every remaining direct query, and replace global unique constraints with tenant-safe constraints before enabling a second school.

- 2026-09-15 PEOPLE ACCESS TENANT MILESTONE COMPLETE:
  - Scoped People & access listings to active school memberships.
  - Blocked cross-school user role/access updates with server-side membership checks.
  - Added school_id to user access audit records.
  - Verification passed: SuperadminAccessTest and SchoolOperationsTest (13 tests, 75 assertions) plus PHP Pint.
  - Remaining safety work: tenant-scope notification workers and all remaining SchoolPortal validation subqueries, then migrate global unique constraints to tenant-safe composite constraints.

- 2026-09-15 NOTIFICATION WORKER TENANT MILESTONE COMPLETE:
  - Tenant-scoped SchoolNotifications events, visible inbox queries, recipients, published records, reminder exams and deletion of processed events.
  - Tenant-scoped NotificationDelivery recovery, notification/preference joins, delivery claiming, sending and exam checks.
  - Legacy null school_id notification rows remain readable only while exactly one school exists; they are excluded once multiple schools are present.
  - Verification passed: NotificationDeliveryTest and SchoolNotificationTest (7 tests, 72 assertions) plus PHP Pint.
  - Remaining safety work: scope all remaining SchoolPortal validation subqueries and migrate global unique constraints to school-aware composite constraints before activating a second school.

- 2026-09-15 TENANT UNIQUE CONSTRAINT MILESTONE COMPLETE:
  - Replaced global unique indexes with school-aware composite indexes for subjects, staff, students, guardians, teaching assignments, attendance, grades, invoices, payments, expenses, payroll, enrollments, invitations and notification deliveries.
  - Existing single-school data remains valid and migration-backed tests pass.
  - Verification passed: SuperadminAccessTest (4 tests, 9 assertions) and PHP Pint.
  - Remaining schema safety work: restructure school_settings and notification-preference primary keys, then scope remaining SchoolPortal validation subqueries before a second school is activated.

- 2026-09-15 PRIMARY-KEY MIGRATION REVIEW:
  - Attempted to rebuild school_settings and notification-preference keys as tenant composite primary keys, but migration-backed tests revealed incompatibilities with legacy fixtures and attendance upsert conflict targets.
  - Reverted that unverified migration before commit.
  - Corrected attendance upsert conflict target to school_id + student_id + date for the already-published tenant-aware attendance index.
  - Verification restored: InterfacePreferencesTest, NotificationDeliveryTest, SchoolOperationsTest and SuperadminAccessTest (20 tests, 110 assertions) plus PHP Pint.
  - Next step: design a compatibility-safe primary-key migration with explicit data backfill and update all legacy write paths before applying it.

- 2026-09-15 TENANT KEY MIGRATION COMPLETE:
  - Rebuilt school_settings with a composite school_id + key primary key.
  - Rebuilt notification preferences with a composite school_id + user_id primary key.
  - Legacy rows may temporarily retain null school_id for single-school compatibility; new application writes use the active tenant.
  - Verification passed: InterfacePreferencesTest, NotificationDeliveryTest, SchoolOperationsTest and SuperadminAccessTest (20 tests, 110 assertions) plus PHP Pint.
  - Multiple-school activation still waits for the remaining SchoolPortal validation-subquery audit and an explicit legacy-null cleanup/NOT NULL migration.

- 2026-09-15 SCHOOLPORTAL VALIDATION AUDIT MILESTONE COMPLETE:
  - Tenant-scoped SchoolPortal validation subqueries for teaching assignments, guardians, exams, enrollments, grades, invoices, payments, payroll, classes, students, timetables and leave conflicts.
  - Attendance upsert now targets the tenant-aware school_id + student_id + date key.
  - Kept strict tenant query helpers; only notification delivery has an explicit one-school legacy-null compatibility path.
  - Verification passed: SchoolOperationsTest, ReportCardTest, SuperadminAccessTest, NotificationDeliveryTest and SchoolNotificationTest (22 tests, 168 assertions) plus PHP Pint.
  - Remaining before second-school activation: audit WhatsApp webhook/console commands, perform legacy null school_id cleanup, enforce NOT NULL ownership, and run complete API isolation coverage.
