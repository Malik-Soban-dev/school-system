# Project Memory & Active Context Tracking

## 1. Current System State
*   **Project Name:** Laravel "school-system"
*   **Current Phase:** Phase 1 (Foundation & Deployment Setup).
*   **Environment:** Windows (Laragon cmd.exe) -> Docker (Render Linux).

## 2. Resolved Autoloader Issue
*   **Issue:** The application previously failed to boot locally during Composer's `post-autoload-dump` phase (specifically during `package:discover`).
*   **Error Trace:** 
    ```text
    Script @php artisan package:discover --ansi handling the post-autoload-dump event returned with error code 255
    Fatal error: Uncaught Error: Class "Illuminate\Foundation\Application" not found in D:\laragon\www\school-system\bootstrap\app.php:8
    ```
*   **Root Cause Analysis:** 
    *   The installed project is Laravel 13.30.1; the bootstrap file is compatible with this version.
    *   The bootstrap file uses Laravel 13's supported `Illuminate.Foundation.Application::configure()` structure.
    *   The Composer autoloader and package metadata were restored.

## 3. Key Decisions & Technical Pivots Log
*   **DECISION 001 (Database):** Adopted Turso (libSQL) instead of MySQL to lower hosting costs and improve edge read latency.
*   **DECISION 002 (Framework Version):** Using Laravel 13.30.1; confirm the Turso adapter supports this version before production deployment.
*   **DECISION 003 (Platform Emulation):** Forced `composer config platform.php 8.4.0` locally. This bypasses Laragon's PHP 8.3 limitation, ensuring production packages (`symfony/clock`) don't fail the Render build.
*   **DECISION 004 (Dependency Purge):** Keep Composer dependencies synchronized with the installed Laravel 13.30.1 release.

## 4. Immediate Next Steps (Action Items)
1.  **Feed Codex:** Provide the detailed `codex-prompt.txt` to the terminal AI agent.
2.  **Validate application:** Completed. `php artisan about`, package discovery, route loading, and the test suite all pass locally.
3.  **Validate Autoloader:** Successfully run `composer dump-autoload --ignore-platform-reqs` without throwing Exit Code 255.
4.  **Render Deployment:** Push the stabilized codebase to GitHub, monitor the Render logs, and verify that `php artisan migrate` executes successfully against the Turso database.




